<% if object.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-danger">
      エラーが<%= pluralize(object.errors.count, "error") %>箇所あります.
    </div>
    <ul>
    <% object.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>