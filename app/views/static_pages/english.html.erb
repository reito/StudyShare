<h1>英語</h1>
<% if @microposts.any? %>
    <h3>投稿数 (<%= @microposts.count %>)</h3>
    <ol class="microposts">
        <%= render @microposts %>
    </ol>
    <%= will_paginate @microposts %>
<% end %>